import 'package:flutter/material.dart';

class NuevoForoScreen extends StatelessWidget {
  static const String name = 'new_forum_screen';

  const NuevoForoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Data"),
      ),
    );
  }
}
